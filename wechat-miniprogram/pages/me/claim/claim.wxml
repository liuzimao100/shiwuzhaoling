<view class="container">
  <view class="header">
    <text class="title">我的认领</text>
    <text class="subtitle">共 {{items.length}} 条认领记录</text>
  </view>

  <view class="empty-container" wx:if="{{items.length === 0}}">
    <view class="empty-icon-wrapper">
      <text class="empty-icon">🔍</text>
    </view>
    <text class="empty-title">暂无认领记录</text>
    <text class="empty-desc">在“丢失物品”中成功匹配的记录\n会保存在这里方便查看</text>
  </view>

  <view class="list-container" wx:else>
    <view class="items-grid">
      <view class="item-card" wx:for="{{items}}" wx:key="{{item.id}}">
        <view class="item-image-wrapper">
          <image class="item-image" src="{{item.imageUrl}}" mode="aspectFill"></image>
        </view>
        <view class="item-info">
          <text class="item-id">编号: {{item.id}}</text>
          <text class="item-time">匹配时间: {{item.createdAt}}</text>
          <text class="item-phone">联系电话: {{item.phone || '暂无'}}</text>
        </view>
      </view>
    </view>
  </view>
</view>
